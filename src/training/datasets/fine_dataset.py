"""Torch dataset for the BBC classification dataset."""


from dataclasses import dataclass
from torch.utils.data import Dataset

@dataclass
class FineDataset(Dataset):
    """Torch dataset for the BBC classification dataset."""

